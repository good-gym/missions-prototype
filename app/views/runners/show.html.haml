.container
  .title-with-button
    %h1= @runner.name
    = login_as_button(@runner)

  %h3 Slots Booked
  .list-group
    = render partial: "availabilities/availability", collection: @runner.availabilities
